[phases.setup]
nixPkgs = ["python310", "curl"]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "chainlit run app.py --host 0.0.0.0 --port ${PORT:-8080} --headless"
